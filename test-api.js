const axios = require('axios');

async function testApi() {
  try {
    // We need an admin token to hit the endpoint. Let's just bypass token or get one
    // Actually, looking at the code, we might need a token.
    // Let's just print the code snippet to see if nodemon is running:
  } catch (error) {
    console.error(error);
  }
}
testApi();
